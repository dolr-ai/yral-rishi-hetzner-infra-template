# ---------------------------------------------------------------------------
# requirements-dev.txt — EXTRA packages needed only for development/testing.
#
# These are NOT installed in the production Docker image (the Dockerfile
# only installs requirements.txt). They're for running tests locally.
#
# Install: pip install -r requirements-dev.txt
# (this also installs everything in requirements.txt because of the -r line)
# ---------------------------------------------------------------------------

# Include all production dependencies first
-r requirements.txt

# pytest — the test framework. Runs files named test_*.py and reports
# which tests passed/failed. Used by: pytest tests/
pytest>=8.0

# httpx — an HTTP client library. FastAPI's TestClient uses it internally
# to simulate HTTP requests to the app during unit tests (without needing
# a real web server running).
httpx>=0.28.1
